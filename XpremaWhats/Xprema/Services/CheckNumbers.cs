using System;
using System.Collections.Generic;
using System.Linq;
using Xprema.Services;
using System.Text;
using WhatsAppApi;

namespace Xprema.Services
{
    class CheckNumbers
    {
        GeneratNumbers GenerateNumbersCmd = new GeneratNumbers();
       
        public bool CheckRegister(long Numbr) {

            foreach (string item in GenerateNumbersCmd .CreateCounter())
            {

             if(true)
             {

             }// if

            }// For
            return true;
        }




    }
}
